[project]
name = "doanmonhoc"
version = "0.1.0"
description = "CLI dich phu de video Anh sang Viet"
requires-python = ">=3.12,<3.13"
dependencies = ["faster-whisper>=1.1", "openai>=1.40", "python-dotenv>=1.0"]

[project.optional-dependencies]
demucs = ["demucs>=4.0"]

[tool.uv]
package = false
